// Add interactivity (if needed)
document.addEventListener("DOMContentLoaded", () => {
    const nameElement = document.getElementById("name");
    nameElement.textContent = "Dallin Carmany"; // Replace with your actual name
});
